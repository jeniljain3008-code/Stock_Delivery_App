"use client";
import { useState } from "react";
import { uploadDeliveryFile } from "@/lib/api";
export default function UploadPage() { const [message, setMessage] = useState(""); return <div className="mx-auto max-w-3xl space-y-6"><h2 className="text-3xl font-bold">Upload NSE Delivery Data</h2><p className="text-slate-400">Upload CSV or Excel with Date, Symbol, Open, High, Low, Close, Volume, DeliveryQty, DeliveryPercent.</p><label className="flex min-h-64 cursor-pointer flex-col items-center justify-center rounded-3xl border border-dashed border-primary/60 bg-card p-8 text-center"><span className="text-lg font-semibold">Drop CSV/XLSX here or click to browse</span><input className="hidden" type="file" accept=".csv,.xlsx,.xls" onChange={async (e) => { const file = e.target.files?.[0]; if (!file) return; setMessage("Uploading..."); try { const res = await uploadDeliveryFile(file); setMessage(`Loaded ${res.rows_loaded} rows from ${res.file_name}`); } catch (err) { setMessage(err instanceof Error ? err.message : "Upload failed"); } }} /></label>{message ? <div className="rounded-xl border border-border bg-card p-4">{message}</div> : null}</div>; }
