import { apiGet } from "@/lib/api";
export default async function SectorsPage() { const rows = await apiGet<any[]>("/api/v1/sectors/rotation"); return <div className="space-y-6"><h2 className="text-3xl font-bold">Sector Rotation Dashboard</h2><div className="grid gap-4 md:grid-cols-2">{rows.map((row) => <div key={row.Sector} className="rounded-2xl border border-border bg-card p-5"><h3 className="text-xl font-bold">{row.Sector}</h3><p className="mt-2">Average delivery increase: {Number(row.average_delivery_increase).toFixed(2)}x</p><p>Leadership index: {Number(row.leadership_index).toFixed(1)}</p><div className="mt-4 h-3 rounded-full bg-slate-800"><div className="h-3 rounded-full bg-primary" style={{ width: `${Math.min(row.leadership_index, 100)}%` }} /></div></div>)}</div></div>; }
