export function KpiCard({ label, value, change }: { label: string; value: string | number; change?: number | null }) { return <div className="rounded-2xl border border-border bg-card p-5 shadow-sm"><p className="text-sm text-slate-400">{label}</p><div className="mt-3 flex items-end justify-between"><span className="text-3xl font-bold">{value}</span>{change !== undefined && change !== null ? <span className="text-sm text-emerald-400">{change}%</span> : null}</div></div>; }
